// Helper function to inspect messages - disabled to reduce log spam
export const inspectMessage = (message: any, context: string = 'Message') => {
  // No-op to reduce log spam
  return;
};

// Helper function to inspect conversations - disabled to reduce log spam
export const inspectConversation = (conversation: any, context: string = 'Conversation') => {
  // No-op to reduce log spam
  return;
};

// Helper function to inspect clients - disabled to reduce log spam
export const inspectClient = (client: any, context: string = 'Client') => {
  // No-op to reduce log spam
  return;
}; 